{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  //},
  //"ConnectionStrings": {
  //  "DefaultConnection": "YOUR_DATABASE_CONNECTION_STRING"
  //},
  //"Admin": {
  //  "Username": "YOUR_ADMIN_USERNAME", // for seeding the first admin user
  //  "Password": "YOUR_ADMIN_PASSWORD" // for seeding the first admin user
  // currently we are passing password through arguments, this is just a placeholder
  //},
  //"SecuritySettings": {
  //  "AdminWhitelist": [ "127.0.0.1", "::1" , "replace with SystemUsers(Admins) IP" ],
  //  "ExternalSystemWhitelist": [ "replace with NeoVoting system IP" ],
  //  "ApiKey": "replace with your api key that NeoVoting needs to access this system"
  //}
}